spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/order_flow_db
    username: luis
    password: luis
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true